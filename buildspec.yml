version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Building the application...
      - gradle build  # Gradle을 사용하여 빌드 실행
artifacts:
  files:
    - build/libs/*.jar  # Gradle로 빌드한 JAR 파일 위치 (기본적으로 build/libs 폴더에 위치)

